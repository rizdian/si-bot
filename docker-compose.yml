services:
  bot:
    build: .
    container_name: si-bot
    environment:
      - DISCORD_TOKEN
      - LOG_CHANNEL_ID
      - OWNER_USER_ID
      - GUILD_ID
      - API_KEY_AI
      - OPENROUTER_MODEL
    restart: unless-stopped
